<%= form_with(model: reverse_look_up, local: true) do |form| %>
  <% if reverse_look_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reverse_look_up.errors.count, "error") %> prohibited this reverse_look_up from being saved:</h2>

      <ul>
      <% reverse_look_up.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :reverse_look_up_title %>
  </div>

  <div class="field">
    <%= form.label :keyWords %>
    <%= form.text_field :keyWords, id: :reverse_look_up_keyWords %>
  </div>

  <div class="field">
    <%= form.label :searchableKeys %>
    <%= form.text_field :searchableKeys, id: :reverse_look_up_searchableKeys %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
